import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.toast.color.test',
  appName: 'ionic-toast-color-test',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
